name: codex_remote_widgetbook
description: Widgetbook app for design_system using mock controllers.
publish_to: 'none'

version: 0.0.1

environment:
  sdk: ^3.10.3

dependencies:
  flutter:
    sdk: flutter
  design_system:
    path: ../design_system
  widgetbook: ^3.0.0
  get: ^4.7.3
  uuid: ^4.5.2
  flutter_chat_core: ^2.9.0
  flutter_chat_types: ^3.6.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
